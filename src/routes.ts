export const PUBLIC_ROUTES = ["/", "/verify"]

export const AUTH_ROUTES = ["/login", "/register", "/error"]

export const AUTH_PREFIX = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT = "/dashboard"
